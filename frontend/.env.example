# Frontend environment (copy to `.env` or use the Vite dev proxy)
# Leave empty to use the relative `/api/v1` base URL through the Vite dev
# proxy (recommended for local development).
#
# Set this when the frontend is served from a different origin than the
# API (e.g. preview/production). Never put backend secrets here.
VITE_API_URL=/api/v1
