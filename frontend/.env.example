# Frontend environment (copy to `.env`)
# Local development: VITE_API_URL points to the local backend (Vite proxy also forwards /api).
# Production (Vercel): set VITE_API_URL to the Render backend URL in the Vercel dashboard.
VITE_API_URL=http://localhost:5000/api/v1
